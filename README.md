# Nexo POS Belgium Releases

This repository contains the public, automated release feed for the Belgium
Nexo POS channel. Client builds retrieve update metadata from
`manifests/latest-v19.json` and download artifacts from GitHub Releases.

Only the `odoo19/belgium` release workflow in `KAD-org/flutter_pos` should
publish releases or update manifests here.
